import { Quote } from "../Quote/quote";

export function QuotesSection(props) {
    return (
      <div>
        <Quote />
        <button></button>
        <button></button>
        <button></button>
      </div>
    );
  }